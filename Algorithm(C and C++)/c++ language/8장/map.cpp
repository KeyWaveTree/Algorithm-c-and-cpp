#include<iostream>
#include<map>

using namespace std;

map<int, int> M;

int main()
{
	//M[5]=3
	//M[5]++
	//M.insert() //key 기준으로 알아서 오름차 순 정렬
	//Map의 구조가 (avl)트리여서 선택된 노드 기준으로 key 값 큰것은 뒤로 작은 값은 앞으로 된다. log n번만에 어느 경우여도 완전이진트리가 된다.

}