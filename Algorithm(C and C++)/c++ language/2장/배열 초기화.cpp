#include<iostream>

//namespace 설명: 같은 함수가 있으면 문제가 있기에 출처를 미리 밝힘

int main()
{
	//가장 중요한것들 : vector, 페어, set , map, 우선순위 큐, 덱 큐, 스텍, 큐, sort, 백터 소트
	//std::fill_n vs std::fill 차이
	//fill_n(범위 시작 주소, 변경하려는 원소 갯수, 변경 값)
	//fill (범위 시작 주소, 변경하려는 원소의 범위 종료 주소, 변경 값)
	
	//보편적 초기화 방법
	//int a[]={} ->int a{};

	//range for (call by value , call by reperence)
}